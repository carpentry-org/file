; Implementations return Result.Success containing the number of values
; written, and may assume the file is writable. File.write enforces this, but
; direct callers of write-to-file must check it themselves.
(definterface write-to-file (Fn [(Ref File) a] (Result Int String)))
