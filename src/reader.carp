; Implementations return Result.Success containing the read value, and may
; assume the file is readable. File.read enforces this, but direct callers of
; read-from-file must check it themselves.
(definterface read-from-file (Fn [(Ref File) Int] (Result a String)))
